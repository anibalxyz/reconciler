package com.anibalxyz.features.auth.application.env;

import com.anibalxyz.features.auth.application.JwtService;
import java.time.Duration;

/**
 * Defines the contract for providing JWT-related environment configuration.
 *
 * <p>This interface acts as a port for environment-dependent settings required by the {@link
 * JwtService}, such as the secret, issuer, and expiration time for access tokens. It decouples the
 * service from the concrete source of configuration.
 *
 * @author Generated by AI
 */
public interface JwtEnvironment {
  /**
   * Returns the secret key used for signing and verifying JWT access tokens.
   *
   * @return The JWT secret key.
   */
  String JWT_SECRET();

  /**
   * Returns the issuer name to be included in the JWT access tokens.
   *
   * @return The JWT issuer name.
   */
  String JWT_ISSUER();

  /**
   * Returns the expiration time for JWT access tokens.
   *
   * @return The access token expiration time.
   */
  Duration JWT_ACCESS_EXPIRATION_TIME_MINUTES();
}
