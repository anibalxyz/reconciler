package com.anibalxyz.features.common.api.out;

import java.util.Collections;
import java.util.List;

/**
 * Represents a standardized error response payload for the API.
 *
 * <p>This record is used to provide consistent, machine-readable error messages to clients for
 * issues like validation failures or server errors.
 *
 * @param error A short, high-level summary of the error.
 * @param details A list of more detailed, human-readable explanations of the error.
 * @author Generated by AI
 */
public record ErrorResponse(String error, List<String> details) {
  /**
   * A convenience constructor to create an {@code ErrorResponse} with no details.
   *
   * @param error A short, high-level summary of the error.
   */
  public ErrorResponse(String error) {
    this(error, Collections.emptyList());
  }
}
